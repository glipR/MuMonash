# Not immediately sure.
# Seems like a binary search (Does there exist a k length unique string?) or sqrt-decomp problem
# sqrt-decomp in particular seems promising, since if there is no sqrt length unique string, we have a looot of repeated chars.
